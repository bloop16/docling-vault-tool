# Entwicklungs-/Testabhaengigkeiten (Laufzeit: siehe requirements.txt).
pytest>=8
# Optional zur Laufzeit (Ereignismodus der Ordnerueberwachung), hier fuer Tests.
watchdog>=4
# Laufzeitabhaengigkeit des Vault-Builders, hier fuer die Tests ohne Docling.
python-frontmatter>=1.0
# Fuer die Embedding-/Similarity-Tests (Laufzeit: Extra [embed]).
numpy>=1.24
